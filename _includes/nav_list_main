{% assign sum = site.posts | size %}

<nav class="nav__list">
    <input id="ac-toc" name="according-toc" type="checkbox" />
    <label for="ac-toc">{{ site.data.ui-text[site.locale].menu_label }}</label>
    <ui class="nav__items" id="category_tag_menu">
        <!-- 전체 글 수 -->
        <li>
            📚 <span style="font-family:monospace">전체 {{sum}}개</span>
        </li>
        <li>
            <!-- 카테고리 분류 -->
            <span class="nav__sub-title">Github Blog</span>
            <!-- 같은 카테고리 페이지 -->
            <ui>
                {% for category in site.categories %}
                    {% if category[0] == "Blog" %}
                        <li><a href="/categories/blog" class="">Github Blog ({{category[1].size}})</a></li>
                    {% endif %}
                {% endfor %}
            </ui>
        </li>
    </ui>
